[project]
name = "stock-analysis-tools"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "jupyter>=1.1.1",
    "matplotlib>=3.10.8",
    "pandas>=3.0.1",
    "plotly>=6.5.2",
    "yfinance>=1.2.0",
]

[dependency-groups]
dev = [
    "pre-commit>=4.5.1",
]
